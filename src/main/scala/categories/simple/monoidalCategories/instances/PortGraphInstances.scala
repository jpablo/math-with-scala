package categories.simple.monoidalCategories.instances

import categories.simple.*
import categories.simple.monoidalCategories.StrictMonoidal

object PortGraphInstances {
  
  // def portGraphCat[B, P] extends StrictMonoidal[PortGraphCat[B, P]]:
  //   type Graph[A, B] = PortGraphCat[B, P][A, B]
  //   def id[A] = ???
  //   extension [A, B, C] (g: Graph[B, C]) def ◦ (f: Graph[A, B]) = ???

}
